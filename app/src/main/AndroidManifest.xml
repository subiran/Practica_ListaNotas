<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ana.listanotas" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ListaNotasAcitvity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddEditNotas"
            android:label="@string/title_activity_add_edit_notas"
            android:parentActivityName=".ListaNotasAcitvity"><!--esto te hace q lista notas activity sea la papi de todas y que cuando estes en addedditnotas te saldrá una flechita arriba a la izquierda que te permita volver hacia atrás-->
        </activity>
    </application>

</manifest>
